import React from 'react';

/**
 * Component Area
 * @param {*} props
 * @returns element
 */

const Area = (props) => {
	// declare structure props

	// eslint-disable-next-line jsx-a11y/alt-text
	return <area {...props}/>;
};

export default Area;
