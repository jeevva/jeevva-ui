import React from "react";

/**
 * Component Wbr
 * @param {*} props
 * @returns element
 */

const Wbr = (props) => {
  // Declare structure props
  const { children } = props;

  return <wbr {...props} />;
};

export default Wbr;
